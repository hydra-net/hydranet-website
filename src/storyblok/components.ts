import DexPage from '../components/Pages/DexPage';
import HomePage from '../components/Pages/HomePage';

/**
 * Used to declare storyblok component, this the mapping is correct for storyblok with the
 * <StoryblokComponent blok={story.content} />; on each pages
 */
export const components = {
  HomePage,
  DexPage,
};
