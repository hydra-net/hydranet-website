export enum Sides {
  left,
  right,
}

export enum Sections {
  'app-top',
  about,
  products,
  roadmap,
  buy,
  articles,
  dex_download,
}

export enum TABS_VALUES {
  'DONE' = 0,
  'IN PROGRESS' = 1,
  'TO DO' = 2,
}

export enum DEX_DOWNLOAD_TABS_VALUES {
  MAINNET,
  TESTNET,
}
