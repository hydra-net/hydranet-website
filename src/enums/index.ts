export enum Sides {
  left,
  right,
}

export enum Sections {
  'app-top',
  about,
  products,
  roadmap,
  buy,
  articles,
}

export enum ROADMAP_TABS_VALUES {
  DONE,
  IN_PROGRESS,
  TODO,
}

export enum DEX_DOWNLOAD_TABS_VALUES {
  MAINNET,
  TESTNET,
}
